//@ts-nocheck
import { Center, Container, Flex, Card } from '@mantine/core';

const DappChainInfo = ({children}) => {
  return (
    
    <Flex>
      {children}
    </Flex>
  )
}

export default DappChainInfo